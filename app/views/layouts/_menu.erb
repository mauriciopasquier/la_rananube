<%# Menú de navegación para la barra superior. %>
<% if user_signed_in? %>
  <div class="dropdown">
    <%= link_to t('.administracion'), '#',
      class: 'nav-item nav-link dropdown-toggle',
      data: { toggle: 'dropdown' } %>

    <div class="dropdown-menu dropdown-menu-right">
      <%= link_to t('.usuaries'), users_path, class: 'dropdown-item' %>
    </div>
  </div>
<% end %>
