<% content_for(:titulo_de_pagina) { t('.titulo', nombre: @user.nombre) } %>

<%= render 'shared/botonera' do %>
  <div class="col-lg">
    <%= link_to t('volver'), :back, class: 'btn btn-outline-secondary' %>
    <%= link_to t('ver', modelo: User.model_name.human), @user, class: 'btn btn-outline-primary' %>
  </div>
<% end %>

<div class="row">
  <div class="col-lg-4">
    <%= render 'form', user: @user %>
  </div>
</div>
